
  const para=document.createElement("P"); 
  console.log(para); const
  text=document.createTextNode("this is third para") 
  para.appendChild(text);
  const ellement=document.getElementById("div1")
  ellement.appendChild(para);


//   document.createElement()this will create a new element
// such tags such as <div> and </div> or <p></p>
//and to create a text inside texts we use the command ceateTextNode this will take the input on text keep it as a string 
// to actually put our information inot some tags such as <div> and </div> or p tage we can use the command ()()()()()(receiver)appendchild(sender)will input the value onto parent element this will make a command such as 
//<p>this is paragraph</p>but thi is just making the paragraph elemt another step is we have to get the parent element we want to place the text on the html page get it inot a variable and then append this onto that element thats it

